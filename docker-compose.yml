services:
  web:
    image: ${DOCKER_USER}/fastapi-backend:${APP_VERSION}
    ports:
      - "8002:8000"
    environment:
      - DATABASE_URL=postgresql://hello_fastapi:hello_fastapi@db/hello_fastapi_dev
      - SECRET_KEY=your-secret-key-change-in-production
      - ALLOWED_ORIGINS=http://${EC2_IP}:5173,http://localhost:5173,http://localhost:8002
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:14-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=hello_fastapi
      - POSTGRES_PASSWORD=hello_fastapi
      - POSTGRES_DB=hello_fastapi_dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hello_fastapi -d hello_fastapi_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  vue-client:
    image: ${DOCKER_USER}/vue-frontend:${APP_VERSION}
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://${EC2_IP}:8002
    depends_on:
      - web

volumes:
  postgres_data:

networks:
  default: 
    name: hello_fastapi
